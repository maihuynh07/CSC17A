#include "Cards.h"

Cards::Cards() {
   
}

/*
// sort cards by suit and save to rankedCards.
void Cards::rankBySuit(){   
    rankedCards.clear();
    set<int>::const_iterator c_itr; 
    for (c_itr = cards.cbegin(); c_itr!= cards.cend();c_itr++){
        rankedCards.insert(((card)(*c_itr)).first,((card)(*c_itr)).second);
    }
}*/
